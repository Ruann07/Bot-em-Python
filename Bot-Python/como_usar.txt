//// Para fazer o codigo funcionar:
/// (Com o python já instalador)
/// Primeiro você deve instalar o pip no seu pc.
/// Logo apos você deve entra como root.
/// Depois você deve executar esses comandos:
	-$ pip install chatterbot 
/// Para executar você deve digitar:
	-$ python machine.py


/// ATENÇAO: quando executa-lo automaticamente ele vai criar um banco de dados
lá vai ficar gravado suas conversas com o bot.
 